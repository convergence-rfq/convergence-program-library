# RFQ

**Setup**

Build, test and deploy.

```bash
anchor build
anchor test
anchor deploy
```

**CLI**

Initialize the protocol.

```bash
./bin/init.ts
```

List RFQs and responses.

```bash
./bin/list.ts
```